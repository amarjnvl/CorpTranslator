/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}",
    ],
    theme: {
        extend: {
            fontFamily: {
                sans: ['Inter', 'sans-serif'],
            },
            colors: {
                // Integrating "sc" (slate) as the primary neutral
                // We can stick to default Slate or refine it.
                // Linear uses very specific zinc/slate tones.
                // We'll rely on Slate for now but maybe implement custom tokens later if needed.
            },
            animation: {
                'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
            }
        },
    },
    plugins: [],
}
